type Product {
    id: Int! 
    no: String!
    name: String!
    desp: String
    product_images: [ProductImage]!
}

type ProductImage {
    id: Int!
    product: Product!
    image: String!
    seq: Int!
    is_main: Int!
}