type Product {
    id: ID! 
    no: String!
    name: String!
    desp: String
    product_images: [ProductImage]!
}

type ProductImage {
    id: ID!
    product: Product!
    image: String!
    seq: Int!
    is_main: Boolean!
}